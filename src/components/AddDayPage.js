import { NavLink } from "react-router-dom"

function AddDayPage() {
  return (
    <div>
      <h1>Add New Day</h1>
      <NavLink to="/"><button>Home</button></NavLink>
    </div>
  );
}

export default AddDayPage;
